from setuptools import setup

setup(name='gym_risk',
      version='0.0.1',
      install_requires=['gym', 
            'networkx',
            'geopandas',
            'descartes',
            ]  # And any other dependencies risk needs
)